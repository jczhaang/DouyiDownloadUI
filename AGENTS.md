# AGENTS.md — 项目记忆

> 本文件是项目的"长期记忆"，供未来的 Codex 会话和开发者本人阅读。
> 每当做出重大决策、改变方向或进入新阶段时，先更新本文件。

## 项目概览

- **项目名称**：DouyiDownloadUI（抖音视频下载与处理工具）
- **项目性质**：学生单人学习项目。目标不是"做得多大"，而是走完一个专业软件从需求到发布的全生命周期
- **目标用户**：老年人（交互必须极简：大字、大按钮、步骤少、容错高、无需说明书）
- **核心功能**：从抖音下载视频（MP4 原样保存），可选提取 MP3
- **目标平台**：Windows 桌面应用

## 学习目标（用户的核心诉求）

用户希望完整学习专业软件开发流程：

1. 持续的版本控制（Git 工作流、规范的 commit、分支策略）
2. 完备的测试（单元/集成测试，尽可能 TDD）
3. CI/CD（自动化测试、构建、发布）
4. Release 管理（语义化版本、Changelog、Release 记录）
5. 好用的 UI（对老年人友好的设计）
6. 完整流程（需求 → 设计 → 计划 → 实现 → 评审 → 发布 → 打包）
7. 最终打包（安装程序、签名、更新机制）

## 协作约定（对 Codex 会话的指令）

- 每个环节先解释"为什么"，再给"怎么做"，帮助用户真正学会
- 涉及学习点时，先让用户思考，再给出专业做法
- 重大决策（技术栈、架构、发布方式等）必须记录在下方"决策记录"
- 遵循既定技能流程：brainstorming（需求）→ writing-plans（计划）→ 实现（TDD）→ 评审 → release
- 用户是学生/初学者：术语第一次出现时用通俗语言解释，避免只给结论不给原因

## AI Agent 协作规范（新功能 / 修 bug 时必须遵守）

1. **先读再动**：完整阅读本文件、HANDOFF.md、设计规格与实施计划后再动手。
2. **需求与设计**：新功能先 brainstorming 确认需求并更新设计规格；多步任务先写实施计划，用户确认后再实现。
3. **Bug 修复**：先用 systematic-debugging 定位根因，禁止猜测式修改。
4. **实现**：强制 TDD——先写失败测试并确认失败，再写最小实现至转绿；每次提交前全量 `dotnet test` 必须全绿。
5. **提交**：Conventional Commits，不夹带无关改动。
6. **评审与合并**：完成/合并前做代码评审；分支集成、合并、打标签、发版本由用户决定，Agent 不得擅自执行。
7. **完成声明**：声称完成/通过前必须运行验证命令并贴出证据。
8. **硬性约束**：目标用户为老年人（极简、大字、全中文、无"家人"文案）；错误文案与设计规格第 6 节逐字一致；文件名规则 `序号 类型 标题`；下载引擎保持在 `IDownloadEngine` 接口后可替换；不做"去水印"承诺。
9. **决策记录**：重大决策（技术栈、架构、流程）必须追加到下方决策记录表并写明原因。
10. **环境**：git 写操作与网络操作需升级权限；推送 GitHub 用 `git -c http.version=HTTP/1.1` 并自动重试；沙箱启动的 GUI 窗口用户不可见，需用户自行双击运行。

## 当前状态（2026-08-04）

- 阶段：v1.2.0 已发布（2026-08-04），新增类型下拉选择功能 + 修复音频下载 bug
- 实现：原 Task 1-17 + 评审修复 + 抖音分享页引擎 + 图文作品支持 + 类型选择器 + 音频下载修复；90 个测试全绿；手动测试（图文+视频+音频）真机通过
- 引擎：抖音分享页解析引擎为主，yt-dlp 兜底（详见决策记录 2026-08-03）
- 仓库：公开 https://github.com/jczhaang/DouyiDownloadUI
- Release：v1.2.0（DouyiDownloadUI-1.2.0-setup.exe + sha256）

## 已知风险与开放问题

- 抖音没有官方公开的下载 API；下载通常依赖解析分享链接或第三方方案，接口可能随时变动，因此下载模块必须设计成可替换
- 下载他人视频涉及版权与平台条款问题，个人学习/自用需注意合规性
- 面向老年人的软件，容错与引导比功能数量更重要

## 决策记录

| 日期 | 决策 | 原因 |
| --- | --- | --- |
| 2026-08-01 | 使用者：老人自己操作（会用电脑，但需极简清晰 UI） | 目标是给老年人用，老人并非完全零基础，UI 走"简洁清晰"而非"零学习"路线 |
| 2026-08-01 | 分发范围：给身边几个亲戚朋友用 | 需要可靠安装包和简单升级，无需商店审核/大规模支持 |
| 2026-08-01 | 运行环境：Win10，电脑性能一般 | 技术选型排除重型框架，要求启动快、占用低 |
| 2026-08-01 | 链接获取：自动识别剪贴板为主 + 手动粘贴兜底 | 老人少做一步，粘贴作为容错 |
| 2026-08-01 | 保存位置：可选并记住上次选择 | 第一次设置好后无需再管，实现简单可靠 |
| 2026-08-01 | 视频处理：MP4 原样保存；"只要音频"= 提取 MP3 | U 盘播放兼容性最好；无压缩/转格式需求 |
| 2026-08-01 | 技术栈：C# / .NET + WPF | Windows 桌面主流专业路线，原生轻量适合卡机；与用户已有的 Java/C++ 基础接近 |
| 2026-08-01 | 下载引擎：yt-dlp（子进程封装，放在可替换接口后） | 无需手动维护抖音 Cookie、社区持续更新；曾评估 Evil0ctal/Douyin_TikTok_Download_API，因需手动维护抖音网页 Cookie 且面向服务器部署而放弃 |
| 2026-08-02 | 文件命名：`序号 类型 标题`（如 `007 中三 广场舞教学.mp4`）；命名窗口的编号输入框可编辑，默认值优先级：①目标文件夹现有最大编号 + 1 → ②上次记忆编号 + 1 → ③1 | 优先按文件夹实际情况接续，避免与已有文件重号；无文件时退回记忆，最终兜底为 1 |
| 2026-08-02 | 导出流程：下载完成后软件"打开文件夹"，操作者手动把文件拖到 U 盘 | 软件无法预知 U 盘已有文件；手动拖拽最直观可靠 |
| 2026-08-02 | 设置页的排查入口改名为"疑难解答"（日志与诊断），不再叫"家人区" | 名称中性，不暗示使用者身份 |
| 2026-08-02 | 非功能需求包含"美观"：界面风格统一、大方 | 用户明确要求；美观影响老年人使用意愿 |
| 2026-08-02 | 链接输入：输入框接受抖音"分享口令"整段文字（非纯链接），软件透明地从中提取 URL；自动识别剪贴板时同样填入整段文字 | 老人从抖音复制的就是整段文字，不需要理解"链接"概念 |
| 2026-08-02 | 主窗口采用"向导步骤 + 最近下载列表"结合：①粘贴链接 → ②确认名字 → ③完成；列表常驻底部 | 每步只做一件事，同时保留下载回顾；确认名字在窗口内完成，不弹小窗口 |
| 2026-08-02 | UI 文案面向"有一定电脑水平的操作者"：错误提示直接说明原因（如"下载引擎异常（yt-dlp 出错）"），全软件不出现"家人"角色；保存位置默认是本地文件夹而非 U 盘 | 操作者不是零基础老人，提示要诚实、可操作；避免引入第三方角色 |
| 2026-08-02 | GitHub 仓库公开，用户名 jczhaang | 用户明确选择公开（简历/社区反馈） |
| 2026-08-02 | 安装包为自包含（self-contained true）发布 | 目标电脑无需预装 .NET 运行时，符合"用户不用装任何东西" |
| 2026-08-02 | 引擎版本锁定 yt-dlp 2026.07.04（记录 SHA256） | 更新是主动行为；工具目录二进制不提交，engine-version.json 提交 |
| 2026-08-03 | 下载引擎策略调整：新增"抖音分享页解析引擎"（手机 UA 读取分享页，解析内嵌 `play_addr` 播放地址与 `desc` 标题），yt-dlp 降级为兜底 | 抖音网页详情接口已封锁 yt-dlp（要求新鲜 Cookie/浏览器签名，官方 issue #16867 确认最新版+有效 Cookie 仍失败）；分享页内嵌播放地址经本地脚本长期验证可用，无需登录/Cookie；视频为带水印 720p（符合"不去水印"范围）；引擎仍可替换 |
| 2026-08-03 | 图文作品支持：检测 `aweme_type=2` 时从 `play_addr.uri` 提取直接 MP3 URL（跳过返回 404 的 `url_list` 转发链接），「下载视频」按钮置灰并提示，「下载音频」直接流式下载背景音乐（无需 ffmpeg） | 用户反馈图文作品下载失败；分析发现图文作品的 `play_addr.url_list` 返回 404，而 `play_addr.uri` 是直接 MP3 URL；方案 B：只下载背景音乐，不下载图片 |
| 2026-08-04 | 类型字段改为下拉选择（ComboBox），选项在设置页增删管理；默认五个：中三、中四、平四、三步、其他 | 用户要求统一类型输入方式，避免手动输入导致的不一致；选项可在设置页自由增删 |
| 2026-08-04 | .csproj 将 ffmpeg.exe 和 yt-dlp.exe 作为 Content 复制到输出目录 | 调试运行时引擎二进制缺失导致音频下载失败（安装版不受影响，Inno Setup 已打包） |
