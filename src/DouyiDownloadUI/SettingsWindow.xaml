<Window x:Class="DouyiDownloadUI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置" Width="480" Height="520" WindowStartupLocation="CenterOwner"
        Icon="/assets/app.ico">
    <StackPanel Margin="20">
        <TextBlock Text="设置" FontWeight="Bold" FontSize="24"/>

        <Border Background="#FFFFFF" BorderBrush="#EEEEEE" BorderThickness="1"
                CornerRadius="8" Padding="14" Margin="0,16,0,0">
            <StackPanel>
                <TextBlock Text="📁 保存位置"/>
                <Grid Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{Binding SaveFolder}"
                               Foreground="#999" TextTrimming="CharacterEllipsis"
                               VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Content="更改" Padding="14,6"
                            Margin="10,0,0,0" Background="#4C8DFF" Foreground="White"
                            Click="ChangeFolder_Click"/>
                </Grid>
            </StackPanel>
        </Border>

        <Border Background="#FFFFFF" BorderBrush="#EEEEEE" BorderThickness="1"
                CornerRadius="8" Padding="14" Margin="0,12,0,0">
            <StackPanel>
                <TextBlock Text="🔠 字体大小"/>
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Content="标准" Padding="18,8" Margin="0,0,10,0"
                            Command="{Binding SetFontSizeCommand}" CommandParameter="Standard"/>
                    <Button Content="大" Padding="18,8" Margin="0,0,10,0"
                            Command="{Binding SetFontSizeCommand}" CommandParameter="Large"/>
                    <Button Content="特大" Padding="18,8"
                            Command="{Binding SetFontSizeCommand}" CommandParameter="ExtraLarge"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Background="#FFFFFF" BorderBrush="#EEEEEE" BorderThickness="1"
                CornerRadius="8" Padding="14" Margin="0,12,0,0">
            <StackPanel>
                <TextBlock Text="🔧 疑难解答"/>
                <TextBlock Margin="0,8,0,0" Foreground="#666"
                           Text="{Binding EngineVersion, StringFormat=下载引擎版本：{0}}"/>
                <TextBlock Margin="0,4,0,0" Foreground="#666"
                           Text="{Binding UpdateStatus}"/>
                <Button Content="复制诊断信息" Padding="14,6"
                        Margin="0,10,0,0" HorizontalAlignment="Left"
                        Command="{Binding CopyDiagnosticsCommand}"/>
                <Button Content="检查更新" Padding="14,6" Margin="0,10,0,0"
                        HorizontalAlignment="Left" Command="{Binding CheckUpdateCommand}"/>
            </StackPanel>
        </Border>

        <Border Background="#FFFFFF" BorderBrush="#EEEEEE" BorderThickness="1"
                CornerRadius="8" Padding="14" Margin="0,12,0,0">
            <StackPanel>
                <TextBlock Text="ℹ️ 关于"/>
                <TextBlock Margin="0,6,0,0" Foreground="#999"
                           Text="版本 1.0.1 · 内置 yt-dlp（Unlicense）与 ffmpeg（GPL），许可声明见安装目录 licenses 文件夹"/>
            </StackPanel>
        </Border>
    </StackPanel>
</Window>
